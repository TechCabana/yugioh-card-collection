# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Test and build output
coverage/
dist/
*.log

# Machine-readable pipeline reports, written by the enrich and sync steps and
# read back by scripts/pipeline-report.mjs. Run artefacts, not source.
.pipeline/

# Planning / local-dev doc — never pushed to remote
CLAUDE.md

# Claude Code slash commands — dev convenience, not needed to run the site
.claude/

# Local design-hook config (impeccable skill) — dev tooling, not runtime
.impeccable/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
